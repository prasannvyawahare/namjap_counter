<?xml version="1.0" encoding="utf-8"?>
<!-- AppTheme.darkCard behind Open — the same treatment the dashboard's
     secondary actions get, so +1 is unmistakably the primary one. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/widget_dark_card" />
    <corners android:radius="14dp" />
</shape>
